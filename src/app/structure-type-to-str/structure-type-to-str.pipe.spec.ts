import {StructureTypeToStrPipe} from './structure-type-to-str.pipe';

describe('StructureTypeToStrPipe', () => {
    it('create an instance', () => {
        const pipe = new StructureTypeToStrPipe();
        expect(pipe).toBeTruthy();
    });
});
